// All task should perform by this folder.

// First Creating a directory.
const fs = require('fs')

// Then name of the Directory
fs.mkdirSync("task_automation")

//After this we have to run the file in terminal . open terminal and write node index.js and the run it .
// and the new directory is there. 